(function($) {
    $.fn.extend({

        fixed_scroll: function() {
            
                          return this.each(function() {
                            
                              var $this = $(this), $t_fixed;

                                    function init() {
                                        
                                        $t_fixed = $this.clone();

                                        
                                    }

                          });
                      }        
    });
})(jQuery);
